/**
 * @bask/ui — shared web components: cards, InsightCard, EvidenceTile, guidance
 * primitives (<Guided>, tour driver, WhisperNote, teaching empty states), chart
 * wrappers. Populated from M0 step 9 onward.
 */

export {};
