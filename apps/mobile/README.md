# @bask/mobile — placeholder

Scaffolded at **M2** (Expo / React Native, one binary, role-switched: Bask Mobile owner shell +
Compass Mobile rep shell — see `docs/IMPLEMENTATION_SPEC.md` §1.1 / §1.3).

Deliberately empty until then: carrying Expo dependencies through M0/M1 buys nothing but install
time and lockfile churn. Nothing here is a pnpm workspace member yet (no `package.json`).
