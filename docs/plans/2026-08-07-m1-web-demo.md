# M1 — Web demo build (the five loops)
## Execution plan. Parents: IMPLEMENTATION_SPEC.md (M1 scope, §0 demo mandate), DESIGN_SPEC.md (visual acceptance bar), PITCH.md (the beats these screens must serve).

**Goal:** the product Nick sees. Every surface in the pitch script, real and interactive, on the M0 spine.

**Visual acceptance bar:** `mockups/*.html` + their PNGs. A screen is not done until a screenshot of it sits beside the mockup without embarrassment — same warmth, hierarchy, restraint. Tokens/components already exist (`@bask/tokens`, `@bask/ui`); do not re-derive them.

**Executor notes:** deviations → `## Deviations` in this file. Steps are intent + constraints; component internals are your judgment. Screenshot every iteration and DM it (`~/tg-dm.sh --file <png> "<context>"`). Assign your lane's dev port (below) — every M0 lane defaulted to 3417 and collided.

## Standing constraints (from M0, do not rediscover)
- Prisma 7 needs `@prisma/adapter-pg`. Migrate MUST use `DIRECT_DATABASE_URL` (:5432) — :6543 pgbouncer hangs silently forever. New migrations: `pnpm db:migration:new` → `pnpm db:check` → `pnpm db:deploy`. Never `prisma migrate dev`.
- Always import the shared client (`import { db } from '@bask/db'`) — never `process.env.DATABASE_URL` in a route. Route-module clients must be lazy.
- ThemeProvider lives in the ROOT layout. Don't add another.
- `packages/core/consent` is the ONE filter every Compass surface reads through. No Compass query may bypass it.
- Insight `evidence` uses the single `Evidence` zod schema from `@bask/core`. UI props read that shape — do not invent a second.
- No auth machinery (explicit non-goal until M3). Roles come from the URL/presenter panel.
- Nothing auto-sends. Guardrail validators run on every generation.
- `pnpm demo:reset` (~32s) then `demo:advance --days N` regenerates state; keep every screen working from a fresh reset.

---

## Lane 1 — Today / Daybreak + shell (port 3421)
Mockup 01 + 05. Real app shell (nav per IA: Today · The Floor · Customers · Marketing · Inventory · Insights), Daybreak letter (Fraunces headline, one italic terracotta emphasis), Attention Queue of `InsightCard`s (severity rail, evidence with bold facts, impact chip, sparkline, fixed action order), pulse rail, Next-up bookings, Weekly Story teaser, location-comparison card for the multi-location org. Card mechanics: *Show me why* expands inline (never a modal), *Dismiss* collapses with an Undo toast and persists state, *Fix this* deep-links to Studio with context.
**Accept:** `/` renders the seeded Daybreak from DB; dismiss survives reload; screenshot sits beside mockup 01; mobile-width (390px) matches mockup 05's arrangement.

## Lane 2 — The Floor (port 3422)
Mockup 02. Promote the M0 `/dev/floor` harness into the real Floor: room board with `.in-session-ring` on in-session rooms, countdowns, ready/cleaning/maintenance chips; check-in panel (search → customer card, membership badge, package n-of-m, session-timing verdict, waiver flag, upsell whisper, service pills, ready-room pills, outcome-stating start button); **real waiver signature capture** (canvas, on the check-in flow); POS-lite (product grid, cart, membership/package/gift-card tenders, simulated tender, receipt state) writing real SaleLines so attachment moves; **USB wedge-scan listener** (timing-signature capture, context-routed); Schedule day/week with drag-rebook and capacity shading; **Shift Handoff** end-of-shift summary card.
**Accept:** check in a customer → session starts on the board → sell a lotion → Insights attachment figure moves. Scan simulation adds to cart. Screenshot beside mockup 02.

## Lane 3 — Studio + Customers (port 3423)
Mockup 03. Studio: entry from an insight (context banner with provenance), goal → audience (smart segments, live counts) → offer (guardrailed) → **real AI generation** of IG post + caption + SMS + email + branded graphic, inline editing, tone pills, regenerate-per-card, phone-frame preview, schedule → campaign lifecycle (draft/scheduled/sent/measured) with results flowing back to Daybreak on clock advance. Customers: list + smart-segment chips, profile (visit timeline, membership/package state, Products tab with repurchase-due), failed-payment recovery flow (pick recoverable → drafted messages → simulated send → recovered state).
**Accept:** the Tuesday campaign beat runs end-to-end; advancing the clock produces measured results; AI generation is real (falls back gracefully if the key is unfunded — log which path ran).

## Lane 4 — Inventory + Insights/Peers (port 3424)
Inventory: stock list with real days-remaining from sell-through, reorder flags, overstock → Studio bridge, scan-receiving, **UVALUX draft order** (recommended lines each with their "because", review screen styled like a real order, send → appears on the Compass account timeline). Insights: what-changed, metric areas, utilization heatmap (hour×weekday from room events), staff view (coaching-framed), campaigns, **activity log**, and **Peers** — cohort chips with n shown, gap framed as opportunity, and the **interactive gap slider** (drag target % → $-impact recomputes live), actions (staff challenge / generate campaign / ask UVALUX for coaching → creates a CoachingRequest visible in Compass).
**Accept:** low-stock insight → draft order → Compass timeline. Gap slider recomputes live and reads smoothly. Every Peers cohort respects the n≥8 suppression.

## Lane 5 — Compass + consent (port 3425)
Mockup 04. Compass dark theme across `/compass`: Network (map + health distribution + 3 folded signal cards), **Call List** (ranked cards, evidence tiles, suggested-conversation blocks, AI call brief in a side sheet, log contact, snooze), Accounts + detail with timeline (draft orders and coaching requests arrive here), Coaching (targets, playbooks, outcomes). Every read goes through `@bask/core/consent`. Plus the Bask-side **"What UVALUX sees"** settings screen: three tiers, side-by-side "what you see / what UVALUX sees" previews, tier flip visibly changes Compass.
**Accept:** flipping a salon's tier in Bask visibly narrows Compass. The private-tier salon shows identity only. Call brief generates. Screenshot beside mockup 04.

## Lane 6 — Public booking + demo harness completion (port 3426)
`/book` public booking page (service → time → confirm, lands on the Floor schedule; pattern-lift from carly-hair-co `/book` + `/s/[slug]`). Presenter Panel completion: all 7 PITCH.md bookmarks staged and working, "seed a walk-in" wired, "fire push" stubbed until M2. `demo:verify` command that drives the full pitch path headlessly (QA agent per repo rules) and fails loudly before a meeting.
**Accept:** `pnpm demo:verify` walks the entire PITCH.md script and reports pass/fail per beat.

---

## Merge protocol (learned from M0)
Lanes own disjoint routes/components. Shared files (root layout, nav, `packages/ui` primitives) belong to Lane 1 — other lanes request additions rather than editing. Merge order 1 → 2 → 3 → 4 → 5 → 6, running `pnpm build && pnpm typecheck && pnpm test && pnpm lint` after each. Re-verify every merged surface in a real browser: M0 proved that three separate bugs were invisible inside their own lanes and only appeared once merged.

## Exit gate
`pnpm demo:verify` green on the full PITCH.md path · every mockup-backed screen screenshotted beside its mockup and DM'd · a non-technical tester completes check-in + campaign creation unaided · all five loops runnable from a fresh `demo:reset` · consent tier flip demonstrably changes Compass · build/typecheck/lint/test green.

## Deviations
- 2026-08-07 · lane 6 (main) · `/book` deferred until lane 2 merges · no Booking/Appointment model exists; Schedule semantics are lane 2's to define, and adding a model mid-flight risks a migration conflict with five running lanes.
- 2026-08-07 · lane 6 (main) · `demo:verify` built first (`scripts/demo-verify.mjs`, `pnpm demo:verify`) · it is the gate that de-risks the pitch and conflicts with nothing. Beats whose surface is unbuilt report SKIP, never PASS. Baseline before M1 lanes merge: 3 pass (demo clock, presenter panel, theme switching), 6 skip, 2 fail (Today has no Daybreak yet — lane 1).
- 2026-08-07 · lane 6 (main) · playwright added as a root devDependency + chromium build 1228 installed · `demo:verify` needs a browser and the repo had none.
- 2026-08-07 · lane 6 (main) · M0 floor harness room-type keys realigned (`uv_lie_down`→`uv_level2`, `spray_booth`→`spray`) · the harness invented keys the fixture generator never seeds, so after any `demo:reset` every room insert failed its foreign key and the board silently rendered zero rooms. Lane 2 replaces this surface; the fix stands so the stand-in is not misleading in the meantime. NOTE for lane 2's merge: `/dev/floor`'s reseed route also returns a cached response (no dynamic marking) — do not carry that pattern into `/floor`.
(log here — date · lane · what changed · why)

- 2026-08-07 · Lane 4 · **Server actions instead of tRPC procedures for Lane 4 writes.** `packages/api/src/routers/domains.ts` is a single file all six lanes would have to edit in the same week. Every Lane 4 write is a form submit on a page Lane 4 owns, so they live in `app/inventory/actions.ts` and `app/insights/actions.ts`. Nothing bypasses the shared client — `db` is always the one from `@bask/db`. The `inventory`/`insights` tRPC namespaces are left as the M0 stubs for whoever needs them on mobile in M2.
- 2026-08-07 · Lane 4 · **Lane 4 components live in `apps/web/src/components/lane4/`, not `@bask/ui`.** The merge protocol gives Lane 1 `packages/ui` for M1. `GapSlider`, `StatRow`, `Chip`, `Sparkline` and `EvidenceSentence` are DESIGN_SPEC §4 vocabulary and are named to match, so promoting them into `@bask/ui` after the merge is a move rather than a rewrite.
- 2026-08-07 · Lane 4 · **Additive entries appended to `packages/ui/src/guidance/guidance.ts`** (4 metrics, 6 tips, 6 whispers, 3 empty states, each under a `Lane 4` comment banner). The dictionary keys are typed unions, so there is no way to key a popover without adding to that file. Appended at the end of each object to keep the merge conflict trivial.
- 2026-08-07 · Lane 4 · **Staff challenges are `ActivityEvent` rows, not a new table.** "Create staff challenge" needs a goal object the Floor can read. Adding a `StaffChallenge` model would mean a Prisma migration against the database all six lanes are sharing this week. The challenge is written as `action: 'staff_challenge_created'` with `{ metric, label, target, windowDays }` in the metadata — which is where the Floor would read it from anyway. Promote to a real table when Lane 2's Floor consumes it.
- 2026-08-07 · Lane 4 · **Peer benchmark contributions are derived, not measured.** PRODUCT_SPEC §20 seeds one operating salon plus eleven dealer accounts that have a health score, an order book and a region but no session rows. Peer values therefore come from documented monotone maps over `account.health_score` (`server/peers.ts`, `PEER_MAPS`), labelled on screen as derived. The subject salon's own side of every comparison is real, and the arithmetic that turns a gap into dollars runs entirely on its real traffic. Cohort membership and suppression are real throughout: `contributesToCohorts` decides who is in, `filterCohort` decides whether the aggregate may be shown.
- 2026-08-07 · Lane 4 · **Catalogue "images" are deterministic colour tiles, not photographs.** No product imagery exists in this dataset and generating fake bottle shots is precisely what DESIGN_SPEC's slop test rules out. `ProductSwatch` derives a hue from the product category and a lightness from the SKU.
- 2026-08-07 · Lane 4 · **Webcam/`BarcodeDetector` scanning not built** (already CUT in IMPLEMENTATION_SPEC §6.2). `/inventory` owns the receiving *destination* — a focused field the wedge scanner types into, plus manual entry. It deliberately installs no global keystroke listener; that is Lane 2's, and two listeners racing for one keystroke burst is a live-demo bug. Unknown codes are logged as `scan_unknown_code`; the "new product?" sheet is M2 with the camera flow.

### Lane 4 findings for the merge
- **`low_stock` fires but does not make the card cut on day `2026-08-11`.** Verified by running the detector directly: it produces *"Cabana Bronzer runs out in about 8 days"* at $203, and `runInsightSweep` keeps `maxInsights: 5` ranked by monthly value — low_stock ranks 7th behind attachment ($9,868), two overstocks ($1,768 / $1,344), spray anomaly ($810) and soft capacity ($502/mo). Not a Lane 4 defect and not a data problem; a ranking property of the M0 sweep at this clock position. `/inventory` surfaces the same finding independently (Cabana Bronzer, 13 on hand, 8.3 days, flagged *Running out*), so the reorder loop does not depend on the card existing.
- **Shared-database hazard.** All six lane worktrees point at the same `bask` schema, so `pnpm demo:reset` in one lane truncates the tables the others are reading. Two lanes resetting at once leaves partially-seeded state that looks like a bug. Reset once, coordinate, and re-verify after any lane resets.
- **Pre-existing hydration warning on every route:** `data-theme="sunset"` on `<html>` differs between server and client render. It comes from the M0 `ThemeScript` in the root layout (Lane 1's file), not from Lane 4 code.
- 2026-08-07 · **Lane 1** · **Skipped `demo:reset` / `demo:advance --days 5`.** The shared CC&SS database was already at exactly that state (`virtual_today` 2026-08-11, briefs for 08-07…08-11, 8 insights, all six detectors fired) and five other lanes were building against it concurrently — a probe caught a `TEST-LANE-C Salon` row and mid-truncate reads from another lane's reset. Resetting would have wiped their in-flight work for no gain. Verified the required state instead of recreating it.
- 2026-08-07 · **Lane 1** · **Insight state transitions are Next.js server actions** (`apps/web/src/app/(bask)/actions.ts`), not tRPC procedures. `packages/api/src/routers/domains.ts` is a file every lane wants to edit; three writes against columns the M0 schema already has do not justify a shared-file conflict. `todayRouter` is left as the M0 stub for whoever needs a typed client (mobile, M2).
- 2026-08-07 · **Lane 1** · **Route group `(bask)/` added** — `/` stays `/`, and every salon-facing surface inherits the shell. Lanes 2–4 should put their routes under `apps/web/src/app/(bask)/` to get the nav for free. `/compass` and `/dev/*` stay outside it.
- 2026-08-07 · **Lane 1** · **`<Guided>` gained an `affordance` prop** (`default | quiet | none`). Four permanent `?` marks down a 320px rail plus one on every chip and sparkline read as clutter next to mockup 01. Quiet/none keep the identical keyboard + screen-reader contract and reveal the mark on hover/focus. Existing `<Guided>` calls are unchanged (`default`).
- 2026-08-07 · **Lane 1** · **Location-comparison card renders a teaching empty state for the seeded multi-location org.** Aurora Collective (the only org with >1 salon) has 0 visits, 0 sales, 0 memberships, 0 customers on both salons — it is seeded as a Compass org shape only. The card, its metric math and its wiring are done; `$0 vs $0` would read as a broken metric, so it teaches instead. **Needs a `packages/db` fixture change (out of this lane) to show real numbers.**
- 2026-08-07 · **Lane 1** · **Mobile keeps a third affordance the mockup does not have.** Mockup 05 is a two-button native card; a browser has no swipe-to-dismiss, so Dismiss survives at 390px as a quiet ghost pushed to the far edge — the row still reads as a button pair. The bottom tab bar likewise carries the real six-destination web IA rather than mockup 05's four native tabs (Daybreak/Pulse/Scan/Campaigns), which are the Expo shell's IA, not the web app's.
- 2026-08-07 · **Lane 1** · **"Next up" reads today's visits after the pulse cut-off hour (11:00 salon-local).** The fixtures carry no future-dated bookings — `session` rows are all `completed` history — so "the rest of today" is the only honest source. When it is empty the card teaches. Lane 2's check-in flow will fill it naturally.
- 2026-08-07 · **Lane 1** · **Bug found in `packages/api/src/context.ts` (NOT fixed from this lane).** `db.salon.findFirst({ where: { OR: [{ slug: x }, { id: x }] } })` throws `invalid input syntax for type uuid` when `?salon=` is a slug — `salon.id` is `@db.Uuid`, and Postgres type-errors rather than returning no rows, taking the whole request down. `apps/web/src/lib/salon-scope.ts` guards with a UUID test; the tRPC context still needs the same guard.
- 2026-08-07 · **Lane 1** · **Pre-existing hydration warning left alone.** `ThemeScript` stamps `data-theme` on `<html>` before paint, which React 19 reports as a server/client attribute mismatch. It reproduces on `/dev/design` (an M0 route) as well as on `/`, so it is root-layout behaviour, not Lane 1's. Cosmetic in dev only — worth a look before the pitch.
- 2026-08-07 · Lane 5 · **Compass components live in `apps/web/src/components/compass/`, not `@bask/ui`.** The merge protocol gives Lane 1 ownership of `packages/ui`, so `EvidenceTile` / `SuggestBlock` / `StatusChip` / `StatRow` / `ConsentBadge` were built to the DESIGN_SPEC §4 names and prop shapes but kept in the app. Promoting them is a file move, not a rewrite. **Request to Lane 1:** take these into `@bask/ui` during merge if the other lanes want them.
- 2026-08-07 · Lane 5 · **Three fields added to `COMPASS_FIELDS.signals`** (`signalType`, `signalHeadline`, `evidenceTiles`). The consent filter drops undeclared keys by design, so rendering the Call List required declaring what it renders — which is the review step the filter exists to force, not a way around it. `evidenceTiles` is a typed, already-derived 3-up rather than the raw metrics blob, so an upstream detector cannot smuggle a new figure into Compass by adding a JSON key.
- 2026-08-07 · Lane 5 · **`dataSharingRouter` mounted at `dataSharing`, not `settings.dataSharing`.** Keeps the consent screen's procedures in the same file as the Compass router they describe; splitting them is how the screen and the filter drift apart.
- 2026-08-07 · Lane 5 · **Fixed an M0 bug in `packages/api/src/context.ts`.** `?salon=<slug>` resolved with `{ OR: [{ slug }, { id }] }`, which sent a non-UUID string to a `uuid` column and made Postgres abort the whole query (`invalid input syntax for type uuid`). Every slug-based salon link 500'd. Now guarded by a UUID test. Touched a shared file because the consent demo beat runs through it.
- 2026-08-07 · Lane 5 · **No standalone Signals destination.** Per the lane brief its three cards are folded into Network, so the sidenav is Network · Call List · Accounts · Coaching. PRODUCT_SPEC §6 still names five destinations; M1 ships four surfaces.
- 2026-08-07 · Lane 5 · **`/compass` is the Call List**, with the shell in a `(app)` route group so M0's `/compass/dev/tokens` harness and its own layout are untouched. The group layout stays a server component supplying a Suspense boundary — `useSearchParams()` without one fails `next build` with a CSR-bailout error.
- 2026-08-07 · Lane 5 · **Call brief ran the deterministic fallback, not the model.** `ANTHROPIC_API_KEY` is present but out of credits (HTTP 400, "credit balance is too low"), so `generateCallBrief` caught it and assembled the brief from the filtered account plus the playbook. The sheet states which path ran, and the server logs `path=… calledApi=… reason=…` per call. The AI path is exercised code, not dead code — it is the same `generateJson` wrapper Daybreak uses.
- 2026-08-07 · Lane 5 · **Bask `/settings/data-sharing` has no nav entry.** Lane 1 owns the shell. **Request to Lane 1:** add Settings → Data sharing. The page works today at `/settings/data-sharing?salon=<slug>`; without the param the M0 context falls back to the oldest salon (Ironwood), not Sunset Ridge.
