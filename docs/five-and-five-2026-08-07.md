# 10 and 10 — uvalux-platform (Bask/Compass)
2026-08-07 · pre-build (specs + mockups stage). Grounding = spec sections, mockup files, donor repos.

## KILLER FEATURES

1. **[industry-standard] Real online booking page in first build, not a teaser** — every incumbent has it; Nick will ask "can customers book?" and Carly's `/book` + `/s/[slug]` public flow is lift-ready. Promote from "represented" tier to simulated-but-interactive. (~half-day) · IMPLEMENTATION_SPEC §7 "booking-page mock" · carly-hair-co `src/app/book`, `/s/[slug]`
2. **[creative] Live push beat in the demo** — presenter panel button fires a real push to the phone in Nick's hand ("Campaign results are in — 9 bookings"). Phone buzzing mid-pitch is the cheapest jaw-drop available. (~1hr once push exists) · IMPLEMENTATION_SPEC §0.1 Presenter Panel + §7 push handlers
3. **[creative] "Tan Safety" as a named, visible engine** — mockup 02 shows "Okay to tan" verdict but no subsystem behind it. Configurable per-province presets (exposure intervals, minors, skin-type). Demo beat: check in someone who tanned 18h ago → friendly block. Huge UVALUX credibility + regulatory story. (~half-day) · DESIGN_SPEC §3.2 session-timing verdict · PRODUCT_SPEC §39
4. **[industry-standard] Gift cards + prepaid packages at POS** — on the brief's table-stakes list, absent from POS-lite scope. Gift card = product type + balance; sells in demo, matters to owners. (~half-day) · PRODUCT_SPEC §18 · IMPLEMENTATION_SPEC §7 `/floor` POS
5. **[boring-overlooked] Owner-facing activity log ("Who did what")** — trust spec requires auditable actions, ConsentProfile has an audit trail, but no screen shows staff actions (discounts, voids, dismissed insights). Owner-operator accountability need. (~half-day) · IMPLEMENTATION_SPEC §2 ConsentProfile · PRODUCT_SPEC §38
6. **[creative] Interactive Peers gap slider** — drag attachment 15%→18%, watch "$X/mo at your traffic" recompute live. Turns the benchmark card into the demo's most tactile moment. (~1hr) · PRODUCT_SPEC §11 example + §13 Peers
7. **[industry-standard] Real waiver signature capture** — currently "pre-signed viewable" simulation; a canvas signature on tablet is trivial and Nick signing one live beats any mock. Digital agreements are an incumbent-listed feature. (~1hr) · IMPLEMENTATION_SPEC §19 simulated tier · mockup 02 waiver flag
8. **[boring-overlooked] Floor offline/degraded mode** — bridge is offline-tolerant but the Floor UI isn't spec'd to survive an internet blip; front desks lose wifi weekly in strip malls. Queue check-ins/sales locally, reconcile on reconnect, visible "reconnecting" whisper. (multi-session) · IMPLEMENTATION_SPEC §5.2 (bridge only) + §1.3 Floor PWA
9. **[creative] Shift Handoff summary** — auto end-of-shift note (sales, incidents, low stock, tomorrow's first three bookings) posted to the Floor. Brief §13 listed "end-of-day summary"; the AI table in PRODUCT_SPEC §16 dropped it — genuinely overlooked, persona D's core need. (~half-day) · PRODUCT_SPEC §25 persona D vs §16 table
10. **[industry-standard] Minimal multi-location comparison card** — persona B is deferred, but the Compass fixtures already seed a multi-location account; one owner-side comparison card (two locations, deltas) unlocks the story with data that already exists. (~half-day) · IMPLEMENTATION_SPEC §19 represented tier · PRODUCT_SPEC §20 portfolio

## STREAMLINES

11. **Cut Linen theme from launch** — 4 themes ship in §4.2; Linen adds contrast-validation + upkeep, zero demo value. Sunset/Dusk/Compass suffice until pilot. (~10min spec edit) · IMPLEMENTATION_SPEC §4.2
12. **Deduplicate the two demo scripts** — PRODUCT_SPEC §21 (12-min) and PITCH.md (15-min, bookmark-mapped) will drift. PITCH.md becomes authoritative; §21 becomes a pointer. (~10min) · PRODUCT_SPEC §21 · docs/pitch/PITCH.md
13. **Drop web BarcodeDetector/ZXing path from M1** — three scan inputs spec'd; webcam-on-laptop is self-described "convenience only." Wedge + expo-camera cover demo and real use. One less fallback library. (~10min spec edit) · IMPLEMENTATION_SPEC §6.2 row 3
14. **Remove `apps/bridge` from M0 scaffold** — empty app until M4; drivers live in `packages/core` and SimulatedDriver runs server-side. Create the app at M4. (~10min) · IMPLEMENTATION_SPEC §1.1
15. **One AI model env var, not three** — `AI_MODEL_BRIEF/CONTENT/CHEAP` is premature config surface; one default + in-code per-call override until real cost data exists. (~10min) · IMPLEMENTATION_SPEC §1.2
16. **No real auth machinery before M3, enforce it** — demo roles via presenter panel/URL param only; auth scaffolding creep during M0–M2 is the likeliest silent scope leak. Add as explicit M0–M2 non-goal. (~10min spec edit) · IMPLEMENTATION_SPEC §19 + §0
17. **Segments: fixed predicates, not query AST** — "definition as stored query AST, live-evaluated" is a multi-session detour; 5–6 coded smart segments cover demo + pilot. AST when a user needs a custom segment. (~10min spec edit, saves multi-session) · IMPLEMENTATION_SPEC §2 Segment
18. **One Evidence shape shared by data model and UI** — `Insight.evidence JSON` and DESIGN_SPEC's `InsightCard`/`EvidenceTile` props should be a single typed schema defined once in `packages/core`, or Opus builds it twice. (~1hr) · IMPLEMENTATION_SPEC §2 Insight · DESIGN_SPEC §4
19. **`mockups/tokens.css` IS packages/tokens v1** — §4.1 specs TS-source→CSS generation machinery; invert it: the CSS file is the source, TS/RN generation added only when Expo needs it (M2). First commit = file copy. (~10min spec edit) · IMPLEMENTATION_SPEC §4.1 · mockups/tokens.css
20. **Fold Compass Signals into Network for M1** — five Compass surfaces in M1 scope, but the pitch never visits Signals (beats 5–7 = Network, Call List, consent). Three seeded signal cards inside Network; full screen at M4. (~10min spec edit) · IMPLEMENTATION_SPEC §19 · PITCH.md beats 5–7
