# TASK — Shot plan v3 (salon intelligence cut)

Write ONE file: `/home/danman60/projects/uvalux-platform/promo/SHOT-PLAN-V3.md`

Read first, in this order:
1. `/home/danman60/projects/uvalux-platform/tasks/opportunity-20260821/CONTRACT.md`
2. `/home/danman60/projects/uvalux-platform/promo/SHOT-PLAN-V2.md` — the format to match
   (per-beat: intent, on-screen action, camera, sound, textures).
3. `/home/danman60/projects/uvalux-platform/promo/VO-SCRIPT-V2.md` — how beats and VO lines pair.

## What this plan is

The build ships new surfaces today; the film re-cuts around them. The story chain every beat
serves: **data → intelligence → dollar opportunity → one-click action → measured outcome**. The
new hero surfaces are:

- `/` — Today transformed into the **Opportunity feed** ("N ways to grow your business today",
  ranked money-first cards, one-click action buttons, outcome cards below).
- `/monitor` — the **Front Desk Monitor** (listener device tile with animated audio bars,
  scored conversation cards, coaching patterns, team table, consent pledge).
- `/customers` — Customer Health (band tiles, health grid, slipping list) — already live.
- `/insights/peers` — the peer scoreboard (percentile tiles, cohort table) — already live.
- `/marketing`, `/inventory/order`, `/compass` — existing surfaces, now entered FROM an
  opportunity card's button rather than toured.

## Format — copy V2's format exactly, with these constraints

- Exactly **10 beats**, each headed `## Beat N — <name>`.
- Each beat has: **Intent** (one line), **On screen** (the composited action, concrete),
  **Camera** (move + duration in frames at 30fps), **Sound** (cue + pin), and a line starting
  exactly `Textures needed:` listing the capture assets by filename (e.g. `today-oppfeed.png`,
  `monitor-full.png`, `monitor-interaction-c1.png`, `customers-grid.png`, `peers-tiles.png`).
- Beats must map 1:1 to the 10 beats of `VO-SCRIPT-V3.md` (same numbering — read the VO task
  spec's beat list in `tasks/opportunity-20260821/queue/20-vo-script-v3.md` if the file itself
  has not landed yet).
- The first line of the file body (under the title) must be:
  `PLAN — generated by the build queue, 2026-08-21. Captures come from the live deploy via
  promo/scripts/capture.mjs; the supervisor adds the new routes to the capture list.`
- Include a closing section `## Capture list additions` naming the routes the capture script
  must add: `/` (opportunity feed state), `/monitor`, `/customers`, `/insights/peers` — with the
  caption OFF note: this cut is VO-only, no caption pass is rendered.
- Note where the one-click moment lands: the camera must hold on an opportunity card's approve
  button press (SMS `Approve & send to 17 customers`) long enough to read it — this is the
  film's money shot.

## Constraints

- NEVER "built for UVALUX" or any implied official partnership.
- No beats for the Floor, POS or booking — that category is out of the pitch.
- Compass/network material appears only inside Beat 9 (the network beat).

## RULES

- Write exactly ONE file: `/home/danman60/projects/uvalux-platform/promo/SHOT-PLAN-V3.md`
- Do NOT create or modify any other file.
- Acceptance: exactly 10 `## Beat` headings; the PLAN line present; at least 10
  `Textures needed:` lines; the strings `/monitor`, `Opportunity`, `customers`, `peers`,
  `capture` and `caption` all appear; "built for UVALUX" does not.
- DO NOT fix bugs or refactor outside this file. If you find one, note it and leave it.
